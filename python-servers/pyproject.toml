[project]
name = "python-servers"
version = "0.1.0"
description = "Benchmark server implementations for BlackSheep and FastAPI"
requires-python = ">=3.13"
dependencies = [
    "blacksheep>=2.5.1",
    "fastapi>=0.128.5",
    "pydantic>=2.12.5",
    "uvicorn>=0.40.0",
]
